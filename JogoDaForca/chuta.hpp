#include <map>
#include <array>
#include <string>

namespace JogoForca {
	void chuta(std::map<char, bool>& chutou, std::array<char, 5>& chutes_errados, std::array<char, 30>& chutes_feitos, std::string& palavra_secreta, int& numeros_de_chutes_errados, int& numeros_de_chutes_feitos);
}
